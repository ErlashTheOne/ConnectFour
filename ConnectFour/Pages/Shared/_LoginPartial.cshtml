

@if (User.Identity.IsAuthenticated)
{
    <li class="nav-item">
        @*@if (Model.PlayerProfilePic != null)
        {
            @Html.DisplayNameFor(model => model.PlayerProfilePic)
        }
        else
        {

        }*@
        <a class="nav-link" asp-area="" asp-page="/Account/Profile">
            <i class="far fa-user-circle"></i>
        </a>
    </li>
    <li class="nav-item">
        <form class="form-inline" asp-page="/Account/Logout" method="post">
            <button type="submit" class="nav-link btn btn-link">Logout</button>
        </form>
    </li>
}
else
{
    <li class="nav-item">
        <a class="nav-link" asp-area="" asp-page="/Register">Register</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" asp-page="/Account/Login">Log In</a>
    </li>
}